# Example Analysis

This gives some examples for running analyses with EvoWeaver. 
Included is an example datafile containing two dendrograms, two sets of colocalization data, and a species tree.
This analysis can be run with a single `EvoWeaver` object if the labels of the tree are labeled with colocalization data,
but in this example the gene organization algorithms are run separately.
